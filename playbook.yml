- hosts: all
  tasks:
    - name: install good shit
      package:
        name:
          - npm
          - vim
          - bind
          - httpd
          - mariadb-server
          - snapd
        state: latest
  roles:
    - snap
    - named
#ln -s /var/lib/snapd/snap /snap
#
